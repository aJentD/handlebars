// Import MySQL connection.
var connection = require("../config/connection.js");

// Methods
function selectAll(req, res) {

}

function insertOne(res, req) {

}

function updateOne(res, req) {
    
}

// Export the orm object for the model (cat.js).
module.exports = orm;
